server:
  port: 8085
springdoc:
  packages-to-scan: com.righttek.es.precio.controller
  pathsToMatch: /v1, /api/es/precio/**


spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/GOTALENT
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      jdbc:
        lob:
         non_contextual_creational: true
